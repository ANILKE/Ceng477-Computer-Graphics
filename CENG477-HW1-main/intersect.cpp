#include "intersect.h"

intersect::intersect( float distance, vector3f normal, int index ,vector3f intersect_point){
    distance=distance;
    normal=normal;
    index=index;
    intersection_point = intersect_point;
}
